---
id: PLUGIN-OVERVIEW-000
title: VCP Mobile 插件代码文档集总览与导航
description: tauri-plugin-vcp-mobile 全部功能模块的代码级说明索引
version: 0.9.14
date: 2026-05-22
docs_count: 10
total_lines: ~3200
---

# VCP Mobile 插件代码文档集总览与导航

> 本知识库覆盖 `src-tauri/plugins/vcp-mobile/` 中全部功能模块的**代码级**说明，与 `docs/ANDROID_PLUGIN_MANAGEMENT.md`（开发规范与指导）互为补充：规范文档回答"应该怎么做"，本文档集回答"代码是怎么做的"。

---

## 文档地图

| 编号 | 文档 | 覆盖源文件 | 行数 | 核心主题 |
|:----:|------|-----------|:----:|----------|
| 01 | [插件初始化与命令路由](01_插件初始化与命令路由.md) | `src/lib.rs` | ~250 | `VcpMobileState`、命令注册表、`setup` 中的 Android 插件注册 |
| 02 | [屏幕常亮控制](02_屏幕常亮控制.md) | `src/screen.rs` | ~300 | Raw JNI 调用 `addFlags`/`clearFlags`、错误处理、桌面端 stub |
| 03 | [流式前台保活服务](03_流式前台保活服务.md) | `src/stream.rs` + `service/StreamKeepaliveService.kt` | ~450 | 引用计数、Agent 名字列表格式化、`NotificationChannel`、Android 14+ 前台服务类型 |
| 04 | [键盘 Insets 管理](04_键盘Insets管理.md) | `KeyboardInsetsManager.kt` | ~300 | `WindowInsetsCompat.Type.ime()` 监听、`evaluateJavascript` 注入、JSON 序列化 |
| 05 | [生命周期桥接](05_生命周期桥接.md) | `LifecycleBridge.kt` | ~280 | `DefaultLifecycleObserver`、resume/pause/stop/config-changed/low-memory 事件 |
| 06 | [权限与系统控制](06_权限与系统控制.md) | `src/system.rs` + `VcpMobilePlugin.kt`（权限部分） | ~350 | `checkAllPermissions`、`requestAndroidPermission`、`moveTaskToBack`、版本分支逻辑 |
| 07 | [Guest JS API](07_Guest JS API.md) | `guest-js/index.ts` | ~200 | 前端调用封装、命令映射、参数与返回类型 |
| 08 | [文件选择与缩略图生成](08_文件选择与缩略图生成.md) | `VcpMobilePlugin.kt`（pickFile + generateNativeThumbnail） | ~400 | Scoped Storage 文件选择、流式拷贝、SHA-256、硬件加速缩略图 |
| 09 | [电池状态管理](09_电池状态管理.md) | `BatteryStatusManager.kt` + `system.rs` + `battery.rs` | ~300 | BatteryManager 六重省电检测、sysfs 流式读取、PluginHandle 桥接 |

---

## 快速决策树

### 按问题类型查找

| 场景 | 目标文档 |
|------|----------|
| 想知道插件注册了哪些命令、命令名怎么映射 | → [01](01_插件初始化与命令路由.md) §2 |
| 屏幕常亮失效或报错 | → [02](02_屏幕常亮控制.md) §3–4 |
| 流式输出时通知不显示或被系统杀掉 | → [03](03_流式前台保活服务.md) §4–5 |
| 键盘弹起时前端布局没正确收缩 | → [04](04_键盘Insets管理.md) §3 |
| 应用切换前后台时状态同步异常 | → [05](05_生命周期桥接.md) §3 |
| 权限请求没反应或返回错误结果 | → [06](06_权限与系统控制.md) §3–4 |
| 前端调用插件方法时报 "command not found" | → [07](07_Guest JS API.md) §2 或 [01](01_插件初始化与命令路由.md) §2 |
| 需要查 Rust → Kotlin 的调用链路 | → [03](03_流式前台保活服务.md) §5 或 [06](06_权限与系统控制.md) §5 |
| Android 上附件选择后怎么传到 Rust？ | → [08](08_文件选择与缩略图生成.md) §2–4 |
| 缩略图是怎么用硬件加速生成的？ | → [08](08_文件选择与缩略图生成.md) §3 |
| Scoped Storage 下怎么读取用户文件？ | → [08](08_文件选择与缩略图生成.md) §2 / §5 |
| 电量显示异常或省电模式检测失效 | → [09](09_电池状态管理.md) §3.1–3.2 |
| 需要查电池优化白名单状态或引导用户设置 | → [09](09_电池状态管理.md) §6 |
| Agent 上下文中的电池温度/充放电状态从哪来 | → [09](09_电池状态管理.md) §5 |

---

## 插件模块依赖关系

```
┌─────────────────────────────────────────────┐
│  01_插件初始化与命令路由                       │
│  (lib.rs: 状态中心 + 命令分发)                │
└─────────────────┬───────────────────────────┘
                  │ manages VcpMobileState
    ┌─────────────┼─────────────┬─────────────┐
    ▼             ▼             ▼             ▼
┌───────┐   ┌─────────┐   ┌─────────┐   ┌─────────┐
│ 02_屏  │   │ 03_流式 │   │ 06_权限 │   │ 07_Guest│
│ 幕常亮 │   │ 保活   │   │ 系统控制│   │ JS API  │
│(screen)│   │(stream) │   │(system) │   │(guest-js)│
└───────┘   └────┬────┘   └────┬────┘   └─────────┘
                 │             │
        ┌────────┘             └────────┐
        ▼                               ▼
┌───────────────┐               ┌───────────────┐
│ service/      │               │ VcpMobilePlugin.kt
│ StreamKeepalive│              │ (权限 & 系统控制
│ Service       │               │  + 文件选择 & 缩略图)
└───────────────┘               └───────────────┘
└───────────────┘

┌─────────────────────────────────────────────┐
│ 独立事件通道（evaluateJavascript）            │
│ ┌─────────────┐    ┌─────────────────────┐  │
│ │ 04_键盘Insets│    │ 05_生命周期桥接      │  │
│ │ Manager     │    │ LifecycleBridge     │  │
│ └─────────────┘    └─────────────────────┘  │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ 09_电池状态管理（PluginHandle 桥接）          │
│  BatteryStatusManager.kt                     │
│  ├── getBatteryLevel()  [BatteryManager]     │
│  └── isPowerSaveMode()  [六重 ROM 兼容]      │
└─────────────────────────────────────────────┘
```

---

## 与规范文档的关系

| 维度 | `docs/plugins/`（本文档集） | `docs/ANDROID_PLUGIN_MANAGEMENT.md`（规范） |
|------|---------------------------|------------------------------------------|
| 内容 | 代码实现细节、接口签名、数据流 | 开发规范、权限准则、选型指导、血训共识 |
| 阅读时机 | 需要理解或修改具体代码时 | 需要评估设计决策、遵守规范、选型插件时 |
| 典型读者 | 维护插件代码的开发者 | 评审架构、新增插件功能、排查权限/通知问题 |

---

## 术语速查表

| 英文术语 | 中文翻译 | 首次出现文档 |
|----------|----------|:------------:|
| `VcpMobileState` | 插件共享状态 | 01 |
| `PluginHandle` | Tauri 插件句柄 | 01 |
| `run_mobile_plugin` | Rust → Kotlin 桥接调用 | 01 |
| Raw JNI | 裸 JNI 调用（非 PluginHandle） | 02 |
| `FLAG_KEEP_SCREEN_ON` | 屏幕常亮标志位 | 02 |
| `active_streams` | 流式服务引用计数列表 | 03 |
| `START_STICKY` | 前台服务被杀后自动重启策略 | 03 |
| `FOREGROUND_SERVICE_TYPE_REMOTE_MESSAGING` | Android 14+ 前台服务类型 | 03 |
| `WindowInsetsCompat.Type.ime()` | 输入法（键盘）区域 Insets | 04 |
| `evaluateJavascript` | WebView 直接执行 JS 的 API | 04 |
| `DefaultLifecycleObserver` | Android 生命周期观察者接口 | 05 |
| `POST_NOTIFICATIONS` | Android 13+ 通知运行时权限 | 06 |
| `READ_MEDIA_IMAGES` | Android 13+ 细分媒体读取权限 | 06 |
| `InvokeArg` | Tauri 插件参数注解 | 06 |
| Guest JS | 插件暴露给前端调用的 JS API 层 | 07 |
| Scoped Storage | Android 分区存储（限制直接文件访问） | 08 |
| ContentResolver | Android 内容解析器（通过 URI 访问文件） | 08 |
| ThumbnailUtils | Android 系统缩略图生成工具类 | 08 |
| `createImageThumbnail` | 硬件加速图片缩略图生成（API 29+） | 08 |
| `inSampleSize` | Bitmap 预采样比例（防 OOM） | 08 |
| `BatteryStatusManager` | 设备电量与省电模式管理器 | 09 |
| `ACTION_BATTERY_CHANGED` | Android 电池状态粘性广播 | 09 |
| `isPowerSaveMode` | 系统省电模式状态（六重 ROM 兼容检测） | 09 |
| `isIgnoringBatteryOptimizations` | 电池优化白名单状态 | 09 |
| `BatteryInfoTool` | 分布式流式电池信息采集工具 | 09 |
| `sysfs` | Linux 内核虚拟文件系统（`/sys/class/power_supply/`） | 09 |

---

> **维护提示**：新增插件功能后，请同步在本文档集新增对应文档，并更新本章节的文档地图与决策树。最后执行 `pnpm memory:refresh` 重新编译项目记忆图谱。
