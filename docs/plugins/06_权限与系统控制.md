---
id: PLUGIN-PERMISSION-006
title: 权限与系统控制
description: 运行时权限检查、请求及系统级控制命令的实现细节
version: 0.9.14
date: 2026-05-21
related_files:
  - src-tauri/plugins/vcp-mobile/src/system.rs
  - src-tauri/plugins/vcp-mobile/android/src/main/java/com/vcp/mobile/VcpMobilePlugin.kt
---

# 权限与系统控制

## 1. 功能概述

提供 Android 运行时权限的查询与请求能力，以及系统级控制命令（返回桌面）。涵盖通知、存储、电池优化三项权限，并处理 Android 版本差异（Android 13+ 细分媒体权限 vs 传统存储权限）。

---

## 2. 代码结构

### Rust 侧

```
src-tauri/plugins/vcp-mobile/src/system.rs (85 lines)
├── PermissionStatus { notification, storage, battery }
├── check_all_permissions<R>(app) -> Result<PermissionStatus, String>
├── request_android_permission<R>(app, p_type: String) -> Result<(), String>
└── move_task_to_back<R>(app) -> Result<(), String>
```

### Kotlin 侧

```
VcpMobilePlugin.kt（权限相关部分）
├── checkAllPermissions(invoke: Invoke)
├── requestAndroidPermission(invoke: Invoke)
├── moveTaskToBack(invoke: Invoke)
├── onPermissionResult(invoke: Invoke)          @PermissionCallback
├── onBatteryOptimizationResult(invoke, result)  @ActivityCallback
└── emitPermissionsToWebView()
│
└── @InvokeArg class RequestPermissionArgs { lateinit var type: String }
```

---

## 3. 权限检查

### 3.1 接口

```rust
#[derive(Serialize, Deserialize)]
pub struct PermissionStatus {
    pub notification: bool,
    pub storage: bool,
    pub battery: bool,
}

#[tauri::command]
pub fn check_all_permissions<R: Runtime>(app: AppHandle<R>) -> Result<PermissionStatus, String>;
```

| 属性 | 说明 |
|------|------|
| 前端命令 | `plugin:vcp-mobile\|check_all_permissions` |
| 参数 | 无 |
| 返回值 | `PermissionStatus` JSON 对象 |
| 桌面端行为 | 始终返回 `{ notification: true, storage: true, battery: true }` |

### 3.2 Kotlin 实现

```kotlin
@Command
fun checkAllPermissions(invoke: Invoke) {
    val pm = activity.getSystemService(Context.POWER_SERVICE) as PowerManager

    val notificationGranted = if (Build.VERSION.SDK_INT >= 33) {
        ContextCompat.checkSelfPermission(activity, android.Manifest.permission.POST_NOTIFICATIONS) == PackageManager.PERMISSION_GRANTED
    } else {
        true
    }

    val storageGranted = if (Build.VERSION.SDK_INT >= 33) {
        ContextCompat.checkSelfPermission(activity, android.Manifest.permission.READ_MEDIA_IMAGES) == PackageManager.PERMISSION_GRANTED
    } else {
        ContextCompat.checkSelfPermission(activity, android.Manifest.permission.READ_EXTERNAL_STORAGE) == PackageManager.PERMISSION_GRANTED
    }

    val batteryOptimizationIgnored = pm.isIgnoringBatteryOptimizations(activity.packageName)

    val result = JSObject()
    result.put("notification", notificationGranted)
    result.put("storage", storageGranted)
    result.put("battery", batteryOptimizationIgnored)
    invoke.resolve(result)
}
```

#### 版本分支详解

| 权限 | Android 13+ (API 33+) | Android 12 及以下 |
|------|----------------------|-------------------|
| **通知** | `POST_NOTIFICATIONS` 运行时检查 | 无需运行时权限，始终 `true` |
| **存储** | `READ_MEDIA_IMAGES` 细分权限 | `READ_EXTERNAL_STORAGE` 传统权限 |
| **电池优化** | `PowerManager.isIgnoringBatteryOptimizations()` | 同上，API 无版本差异 |

> **注意**：电池优化检查**不是**标准的 Android 运行时权限模型，而是通过 `PowerManager` API 查询应用是否被列入系统电池优化白名单。

---

## 4. 权限请求

### 4.1 接口

```rust
#[tauri::command]
pub fn request_android_permission<R: Runtime>(app: AppHandle<R>, p_type: String) -> Result<(), String>;
```

| 属性 | 说明 |
|------|------|
| 前端命令 | `plugin:vcp-mobile\|request_android_permission` |
| 参数 | `p_type: String` —— 权限类型，支持 `"notification"` / `"storage"` / `"battery"` |
| 返回值 | `Result<(), String>` —— 成功返回 `Ok(())`，请求发起即算成功 |
| 桌面端行为 | 空操作 |

### 4.2 Kotlin 实现

```kotlin
@TauriPlugin(permissions = [
    Permission(strings = ["android.permission.POST_NOTIFICATIONS"], alias = "notification"),
    Permission(strings = ["android.permission.READ_MEDIA_IMAGES"], alias = "storage"),
    Permission(strings = ["android.permission.READ_EXTERNAL_STORAGE"], alias = "storageLegacy")
])
class VcpMobilePlugin(private val activity: Activity) : Plugin(activity) {
    // ...
}
```

`@TauriPlugin` 注解中通过 `@Permission` 定义了三个**权限别名**，将 Android 权限字符串映射为易用的别名：

| 别名 | Android 权限字符串 | 适用版本 |
|------|-------------------|----------|
| `notification` | `POST_NOTIFICATIONS` | Android 13+ |
| `storage` | `READ_MEDIA_IMAGES` | Android 13+ |
| `storageLegacy` | `READ_EXTERNAL_STORAGE` | Android 12 及以下 |

#### 权限请求命令

```kotlin
@Command
fun requestAndroidPermission(invoke: Invoke) {
    val args = invoke.parseArgs(RequestPermissionArgs::class.java)
    when (args.type) {
        "notification" -> {
            if (Build.VERSION.SDK_INT >= 33) {
                requestPermissionForAlias("notification", invoke, "onPermissionResult")
            } else {
                emitPermissionsToWebView()
                invoke.resolve()
            }
        }
        "storage" -> {
            if (Build.VERSION.SDK_INT >= 33) {
                requestPermissionForAlias("storage", invoke, "onPermissionResult")
            } else {
                requestPermissionForAlias("storageLegacy", invoke, "onPermissionResult")
            }
        }
        "battery" -> {
            try {
                val intent = Intent(Settings.ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS).apply {
                    data = Uri.parse("package:${activity.packageName}")
                }
                startActivityForResult(invoke, intent, "onBatteryOptimizationResult")
            } catch (e: Exception) {
                val intent = Intent(Settings.ACTION_IGNORE_BATTERY_OPTIMIZATION_SETTINGS)
                startActivityForResult(invoke, intent, "onBatteryOptimizationResult")
            }
        }
    }
}
```

| 权限类型 | 请求机制 | 回调 |
|----------|----------|------|
| `notification` | `requestPermissionForAlias("notification", invoke, "onPermissionResult")` | `@PermissionCallback` |
| `storage` | `requestPermissionForAlias("storage"/"storageLegacy", invoke, "onPermissionResult")` | `@PermissionCallback` |
| `battery` | `startActivityForResult(invoke, intent, "onBatteryOptimizationResult")` | `@ActivityCallback` |

#### 回调与事件推送

```kotlin
@PermissionCallback
fun onPermissionResult(invoke: Invoke) {
    emitPermissionsToWebView()
    invoke.resolve()
}

@ActivityCallback
fun onBatteryOptimizationResult(invoke: Invoke, result: ActivityResult) {
    emitPermissionsToWebView()
    invoke.resolve()
}

private fun emitPermissionsToWebView() {
    val pm = activity.getSystemService(Context.POWER_SERVICE) as PowerManager

    val notificationGranted = if (Build.VERSION.SDK_INT >= 33) {
        ContextCompat.checkSelfPermission(activity, android.Manifest.permission.POST_NOTIFICATIONS) == PackageManager.PERMISSION_GRANTED
    } else {
        true
    }

    val storageGranted = if (Build.VERSION.SDK_INT >= 33) {
        ContextCompat.checkSelfPermission(activity, android.Manifest.permission.READ_MEDIA_IMAGES) == PackageManager.PERMISSION_GRANTED
    } else {
        ContextCompat.checkSelfPermission(activity, android.Manifest.permission.READ_EXTERNAL_STORAGE) == PackageManager.PERMISSION_GRANTED
    }

    val batteryOptimizationIgnored = pm.isIgnoringBatteryOptimizations(activity.packageName)

    val json = """{"notification":$notificationGranted,"storage":$storageGranted,"battery":$batteryOptimizationIgnored}"""
    val script = "window.dispatchEvent(new CustomEvent('vcp-permission-change', { detail: $json }))"
    webViewRef?.evaluateJavascript(script, null)
}
```

- **`requestPermissionForAlias`**：调用 Tauri 框架封装的 `registerForActivityResult(ActivityResultContracts.RequestMultiplePermissions())`，生命周期安全，无需手动处理 `onRequestPermissionsResult`。
- **`startActivityForResult`**：同理使用 Tauri 框架的 `ActivityResultLauncher`，用户从电池优化设置页返回后触发 `@ActivityCallback`。
- **`emitPermissionsToWebView`**：回调内重新查询全部三项权限状态，通过 `evaluateJavascript` 注入 `vcp-permission-change` CustomEvent，前端 `window.addEventListener` 即可实时接收。

> **注意**：电池优化请求返回的 `ActivityResult.resultCode` 因 ROM 而异（部分系统对话框不返回标准 `RESULT_OK`），因此回调内**不依赖 resultCode**，始终执行 `checkAllPermissions` 逻辑并以实际查询结果为准。

---

## 5. 返回桌面

### 5.1 接口

```rust
#[tauri::command]
pub fn move_task_to_back<R: Runtime>(app: AppHandle<R>) -> Result<(), String>;
```

| 属性 | 说明 |
|------|------|
| 前端命令 | `plugin:vcp-mobile\|move_task_to_back` |
| 参数 | 无 |
| 桌面端行为 | 空操作 |

### 5.2 Kotlin 实现

```kotlin
@Command
fun moveTaskToBack(invoke: Invoke) {
    activity.moveTaskToBack(true)
    invoke.resolve()
}
```

- **`moveTaskToBack(true)`**：将当前任务移到后台，参数 `true` 表示如果当前任务是根 Activity，则整个任务进入后台；`false` 则仅当前 Activity。
- **效果**：等同于用户点击 Home 键，应用仍在运行，不触发 `onDestroy()`。

---

## 6. 数据流完整链路

### notification / storage（标准运行时权限）

```
前端: invoke('request_android_permission', { type: 'notification' })
    │
    ▼
system.rs :: request_android_permission("notification")
    │
    ▼
run_mobile_plugin("requestAndroidPermission", { "type": "notification" })
    │
    ▼
Kotlin: VcpMobilePlugin.requestAndroidPermission(invoke)
    │
    ├── parseArgs(RequestPermissionArgs) → { type: "notification" }
    ├── Build.VERSION.SDK_INT >= 33 ?
    │   ├── YES: requestPermissionForAlias("notification", invoke, "onPermissionResult")
    │   └── NO: emitPermissionsToWebView() + invoke.resolve()
    │
    ▼
Tauri PluginManager :: requestPermissionsLauncher.launch([POST_NOTIFICATIONS])
    │
    ▼
系统弹出权限请求对话框（Android 13+）
    │
    ▼
用户选择 允许/拒绝
    │
    ▼
Tauri 框架回调 → @PermissionCallback onPermissionResult(invoke)
    │
    ├── emitPermissionsToWebView()
    │   ├── 查询 notification/storage/battery 最新状态
    │   └── evaluateJavascript("window.dispatchEvent(new CustomEvent('vcp-permission-change', { detail: {...} }))")
    │
    ▼
前端 window.addEventListener('vcp-permission-change') 收到事件
    │
    ▼
PermissionGate.vue 状态实时刷新为 "OK"
```

### battery（设置页跳转）

```
前端: invoke('request_android_permission', { type: 'battery' })
    │
    ▼
Kotlin: requestAndroidPermission → startActivityForResult(intent, "onBatteryOptimizationResult")
    │
    ▼
系统弹出电池优化对话框 / 或跳转设置页
    │
    ▼
用户操作后返回应用
    │
    ▼
Tauri 框架回调 → @ActivityCallback onBatteryOptimizationResult(invoke, result)
    │
    ├── emitPermissionsToWebView() → 推送 vcp-permission-change 事件
    │
    ▼
前端状态实时刷新
```

---

## 7. 关键约束

1. **`p_type` 字符串硬编码**：前端传入 `"notification"` / `"storage"` / `"battery"`，拼写错误会导致 Kotlin `when` 分支未命中，静默无操作。
   - **防范**：前端封装为枚举或常量，如 `PermissionType.NOTIFICATION`。

2. **事件通道依赖 `webViewRef`**：`emitPermissionsToWebView()` 需要在 `load(webView)` 时保存 WebView 引用。若插件加载失败或 WebView 未初始化，事件将静默丢失（前端仍可通过 `visibilitychange` 和兜底轮询恢复）。

3. **与规范文档的交叉引用**：
   - `POST_NOTIFICATIONS` 血训：参见 `docs/ANDROID_PLUGIN_MANAGEMENT.md` §3.1 / §7.2。
   - Tauri 权限回调机制官方文档：参见 `docs/ANDROID_PLUGIN_MANAGEMENT.md` §5.2。
