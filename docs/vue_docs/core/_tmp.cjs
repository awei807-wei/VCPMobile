const fs = require('fs');
const path = 'docs/vue_docs/core/03_会话状态与消息流.md';
const content = `\n### 1.3 在整体架构中的位置\n\n` + 
`\`\`\`\n` +
`┌─────────────────────────────────────────────────────────────────────┐\n` +
`│                        Vue 3 前端层                                  │\n` +
`│                                                                      │\n` +
`│   ┌─────────────────────────────────────────────────────────────┐   │\n` +
`│   │                    ChatView.vue                              │   │\n` +
`│   │  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │   │\n` +
`│   │  │MessageRenderer│ │InputEnhancer│  │  useChatScroll      │  │   │\n` +
`│   │  │  (消息渲染)   │  │  (输入增强)  │  │  (滚动管理)         │  │   │\n` +
`│   │  └─────────────┘  └─────────────┘  └─────────────────────┘  │   │\n` +
`│   └──────────────────────────┬──────────────────────────────────┘   │\n` +
`│                              │ reactive subscription                  │\n` +
`│   ┌──────────────────────────┴──────────────────────────────────┐   │\n` +
`│   │                    Pinia Store 层                             │   │\n` +
`│   │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │   │\n` +
`│   │  │chatSession  │  │chatHistory  │  │chatStream   │          │   │\n` +
`│   │  │  Store       │  │  Store       │  │  Store       │          │   │\n` +
`│   │  │(会话路由)    │◄─┤(消息历史)   │◄─┤(流式状态)   │          │   │\n` +
`│   │  └─────────────┘  └─────────────┘  └─────────────┘          │   │\n` +
`│   │         ▲                ▲                ▲                  │   │\n` +
`│   │         └────────────────┴────────────────┘                  │   │\n` +
`│   │                      attachmentStore                         │   │\n` +
`│   │                     (附件暂存)                                │   │\n` +
`│   └──────────────────────────┬──────────────────────────────────┘   │\n` +
`│                              │ IPC (Tauri Commands + Channel)         │\n` +
`└──────────────────────────────┼──────────────────────────────────────┘\n` +
`                               │\n` +
`┌──────────────────────────────▼──────────────────────────────────────┐\n` +
`│                      src-tauri (Rust 核心层)                         │\n` +
`│                                                                      │\n` +
`│   ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────┐    │\n` +
`│   │agent_chat_  │  │group_chat_  │  │message_service.rs       │    │\n` +
`│   │application_ │  │application_ │  │(append/load/patch/delete)│    │\n` +
`│   │service.rs   │  │service.rs   │  └─────────────────────────┘    │\n` +
`│   └─────────────┘  └─────────────┘              │                   │\n` +
`│          │                  │                   │                   │\n` +
`│          └──────────────────┴───────────────────┘                   │\n` +
`│                              │                                      │\n` +
`│                    ┌─────────┴─────────┐                           │\n` +
`│                    ▼                   ▼                           │\n` +
`│         ┌─────────────┐     ┌─────────────────┐                   │\n` +
`│         │vcp_client.rs│     │aurora_pipeline  │                   │\n` +
`│         │(SSE 流式)    │     │(语义沉淀)        │                   │\n` +
`│         └─────────────┘     └─────────────────┘                   │\n` +
`│                                                                      │\n` +
`└─────────────────────────────────────────────────────────────────────┘\n` +
`\`\`\`\n`;
fs.appendFileSync(path, content, 'utf8');
console.log('ok');
