---
id: VUE-CHAT-011
title: 消息块与富媒体渲染
description: VCP Mobile 前端消息内容的结构化块渲染——思考块、工具块、HTML 预览与代码高亮
version: 0.9.14
date: 2026-05-27
---

# 11. 消息块与富媒体渲染

## 1. 概述

### 1.1 领域定位

本模块负责将 Rust 后端解析后的结构化消息内容（`ContentBlock[]`）渲染为可视化的 UI 组件。它是 VCP Mobile 聊天体验的核心呈现层，覆盖 Markdown 文本、代码高亮、数学公式、Mermaid 图表、HTML 预览、思考链、工具调用结果等全部富媒体形态。

### 1.2 模块构成表

| 文件/模块 | 类型 | 职责 |
|-----------|------|------|
| `src/features/chat/MessageRenderer.vue` | Vue 组件 | 消息级渲染入口：Block 分发、流式状态管理、重内容渲染（KaTeX/Mermaid） |
| `src/features/chat/blocks/ThoughtBlock.vue` | Vue 组件 | 思考链（Thought/Think）的可折叠块渲染 |
| `src/features/chat/blocks/ToolBlock.vue` | Vue 组件 | 工具调用（tool-use）与工具结果（tool-result）的展示 |
| `src/features/chat/blocks/HtmlPreviewBlock.vue` | Vue 组件 | HTML 代码块的内联预览与全屏沙箱 |
| `src/core/utils/astRenderer.ts` | TS 工具 | AST 节点树 → HTML 字符串的转换器，含 LRU 缓存 |
| `src/core/composables/useContentProcessor.ts` | Composable | 内容处理双模管线：静态 Rust 解析 / 流式前端正则替换 |
| `src/assets/message-blocks.css` | CSS | 全局消息块样式：Markdown、表格、代码、数学、日记、表情 |
| `src-tauri/src/vcp_modules/chat/content_parser.rs` | Rust 模块 | 全量内容解析器：原始文本 → `ContentBlock[]` |
| `src-tauri/src/vcp_modules/chat/stream_block_parser.rs` | Rust 模块 | 流式增量解析器：原始文本 → `StreamBlock[]` |
| `src-tauri/src/vcp_modules/chat/pre_renderer/code_highlighter.rs` | Rust 模块 | 基于 `syntect` 的后端代码高亮 |

### 1.3 在整体架构中的位置

```
┌─────────────────────────────────────────────────────────────┐
│                      Vue 3 渲染层                            │
│  ┌─────────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐ │
│  │ ThoughtBlock│  │ ToolBlock│  │HtmlPreview│  │Markdown  │ │
│  │   .vue      │  │  .vue    │  │Block.vue │  │ (v-html) │ │
│  └──────┬──────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘ │
│         └──────────────┴─────────────┴─────────────┘        │
│                         ▲                                   │
│              MessageRenderer.vue (Block 分发)               │
│                         │                                   │
│              astRenderer.ts (AST → HTML)                    │
└─────────────────────────┬───────────────────────────────────┘
                          │ IPC (Tauri Commands / Events)
┌─────────────────────────┼───────────────────────────────────┐
│                     Rust 核心层                              │
│  ┌──────────────────────┼────────────────────────────────┐  │
│  │   content_parser.rs  │   stream_block_parser.rs       │  │
│  │   (全量静态解析)       │   (流式增量解析)                │  │
│  └──────────────────────┴────────────────────────────────┘  │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  pre_renderer/ (markdown_ast.rs + code_highlighter.rs) │ │
│  │  → AST 生成 + syntect 代码高亮                           │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### 1.4 核心设计原则

1. **Backend-Driven Rendering**：消息内容的生命周期由后端驱动。静态消息经 Rust `parse_content` 预编译为 `ContentBlock[]` + `MarkdownNode[]` AST；流式消息经 `StreamBlockParser` 增量沉淀为 `StreamBlock[]`。前端**不做 Markdown 全量解析**，只做 AST → HTML 的轻量转换。

2. **双模管线（Hybrid Pipeline）**：
   - **静态模式**：`useContentProcessor.processMessageContent` → `invoke("process_message_content")` → Rust 全量解析 → 返回 `ContentBlock[]`。
   - **流式模式**：前端 `transformSpecialBlocksForStream` 做轻量正则替换，将特殊块（thought/tool）转为内联 HTML 占位，避免流式期间频繁调用 Rust。

3. **渲染性能优先**：
   - `astRenderer.ts` 对 AST → HTML 结果做 LRU 缓存（上限 500 条）。
   - `MessageRenderer.vue` 对稳定块使用 `v-memo`，确保已闭合块零开销重渲染。
   - KaTeX / Mermaid / 表情替换等重操作仅在消息**离开活跃流后**统一执行一次。

4. **安全沙箱**：HTML 预览块使用 `DOMPurify` 净化 + `iframe sandbox` 隔离，禁止 `applet`/`embed`/`object` 等危险标签。

---

## 2. 消息块类型系统

### 2.1 ContentBlock 接口详解

前端类型定义位于 `src/core/types/chat.ts`（第 41–68 行）：

```ts
export interface ContentBlock {
  type:
    | "markdown"
    | "tool-use"
    | "tool-result"
    | "diary"
    | "thought"
    | "button-click"
    | "html-preview"
    | "role-divider"
    | "style"
    | "math";
  content?: string;
  nodes?: MarkdownNode[];        // 仅 markdown/diary/thought 携带 AST
  tool_name?: string;
  is_complete?: boolean;         // thought / tool-use 的闭合状态
  status?: string;               // tool-result 的执行状态
  details?: Array<{ key: string; value: string }>; // tool-result 详情
  footer?: string;
  maid?: string;                 // diary
  date?: string;                 // diary
  theme?: string;                // thought
  role?: string;                 // role-divider
  is_end?: boolean;              // role-divider
  display_mode?: boolean;
  highlighted_content?: string;  // html-preview 的后端高亮代码
  hash?: string | number;        // 稳定哈希，用于缓存键
}
```

Rust 后端对应定义位于 `src-tauri/src/vcp_modules/chat/content_parser.rs`（第 8–84 行），结构完全一致，通过 `#[serde(tag = "type")]` 实现 tagged enum 序列化。

### 2.2 Block 类型枚举

| 类型 | 来源标记 | 关键字段 | 渲染方式 |
|------|----------|----------|----------|
| `markdown` | 默认/无标记 | `nodes: MarkdownNode[]` | `astRenderer.renderMarkdownNodes` → `v-html` |
| `tool-use` | `<<<[TOOL_REQUEST]>>>` | `tool_name`, `content`, `is_complete` | `ToolBlock.vue`（type=tool-use） |
| `tool-result` | `[[VCP调用结果信息汇总:` | `tool_name`, `status`, `details[]`, `footer` | `ToolBlock.vue`（type=tool-result） |
| `thought` | `[--- VCP元思考链 ---]` 或 `<think>` | `theme`, `content`, `nodes[]`, `is_complete` | `ThoughtBlock.vue` |
| `diary` | `<<<DailyNoteStart>>>` | `maid`, `date`, `content`, `nodes[]` | `MessageRenderer` 内联模板 |
| `html-preview` | ````html` 或 `<!DOCTYPE html>` | `content`, `highlighted_content` | `HtmlPreviewBlock.vue` |
| `role-divider` | `<<<[ROLE_DIVIDE_XXX]>>>` | `role`, `is_end` | `MessageRenderer` 内联模板 |
| `style` | `<style>...</style>` | `content`（CSS 文本） | 注入 `<head>` 作为作用域样式 |
| `button-click` | `[[点击按钮:...]]` | `content` | `MessageRenderer` 内联模板 |

### 2.3 与后端 Block 类型的映射

```
前端 ContentBlock        Rust ContentBlock (content_parser.rs)      Rust StreamBlock (stream_block_parser.rs)
─────────────────────────────────────────────────────────────────────────────────────────────────────────────
markdown               ←  ContentBlock::Markdown { nodes, hash }  ←  StreamBlock::Markdown { nodes, hash }
tool-use               ←  ContentBlock::ToolUse { tool_name, ... } ←  StreamBlock::Tool { tool_name, ... }
tool-result            ←  ContentBlock::ToolResult { ... }        ←  StreamBlock::ToolResult { ... }
thought                ←  ContentBlock::Thought { theme, ... }     ←  StreamBlock::Thought { theme, ... }
diary                  ←  ContentBlock::Diary { maid, date, ... } ←  StreamBlock::Diary { ... }
html-preview           ←  ContentBlock::HtmlPreview { ... }       ←  StreamBlock::HtmlPreview { ... }
role-divider           ←  ContentBlock::RoleDivider { ... }       ←  StreamBlock::RoleDivider { ... }
style                  ←  ContentBlock::Style { ... }             ←  StreamBlock::Style { ... }
button-click           ←  ContentBlock::ButtonClick { ... }       ←  StreamBlock::ButtonClick { ... }
```

**关键差异**：
- `ContentBlock` 用于**已持久化**的消息历史，`hash` 为 `Option<u64>`。
- `StreamBlock` 用于**流式增量推送**，`hash` 为 `String`（由 `HashAggregator` 计算）。
- 流式期间未闭合的块不会进入 `stable_blocks`，而是作为 `tail_content` / `tail_block` 进行**推测渲染（Speculative Rendering）**。

---

## 3. 思考块（ThoughtBlock.vue）

### 3.1 职责定位

`ThoughtBlock.vue` 负责渲染模型的思考过程（Reasoning Chain）。支持两种来源：
- **VCP 元思考链**：`[--- VCP元思考链: 主题 ---]...内容...[--- 元思考链结束 ---]`
- **标准 Think 标签**：`<think>...</think>` 或 `<thinking>...</thinking>`

两者在 Rust 端均被归一化为 `ContentBlock::Thought` / `StreamBlock::Thought`，`theme` 字段分别为自定义主题或默认 `"思维链"`。

### 3.2 折叠/展开交互

```vue
<!-- src/features/chat/blocks/ThoughtBlock.vue -->
<div class="vcp-thought-header" @click="toggleExpand">
  <span class="vcp-thought-icon">🧠</span>
  <span class="vcp-thought-label">
    {{ block.theme || "元思考链" }}
    <Loader2 v-if="!block.is_complete" :size="10" class="animate-spin" />
  </span>
  <component :is="isExpanded ? ChevronUp : ChevronDown" />
</div>

<div v-show="isExpanded" class="vcp-thought-content animate-slide-down">
  <div class="thought-body"
    v-html="block.nodes?.length > 0
      ? renderMarkdownNodes(block.nodes, messageId)
      : escapeHtml(block.content || '')" />
</div>
```

- **默认折叠**：思考块默认收起，仅展示头部标签，避免占用过多垂直空间。
- **未完成状态指示**：`!block.is_complete` 时显示旋转的 `Loader2` 图标，提示思考仍在进行。
- **内容渲染优先级**：优先使用后端预渲染的 `nodes`（AST），否则回退到 HTML 转义后的纯文本。

### 3.3 样式与动画

```css
/* ThoughtBlock.vue <style scoped> */
.vcp-thought-block {
  background: rgba(0, 0, 0, 0.03);
  border-radius: 12px;
  border: 1px solid rgba(0, 0, 0, 0.1);
  font-size: 0.92em;
  transition: background-color 0.3s ease, border-color 0.3s ease;
}

.animate-slide-down {
  animation: slideDown 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

@keyframes slideDown {
  from { opacity: 0; transform: translateY(-10px); }
  to   { opacity: 1; transform: translateY(0); }
}
```

- **暗色适配**：通过 `html.dark .vcp-thought-block` 切换为 `rgba(120, 120, 128, 0.05)` 背景。
- **动画曲线**：`cubic-bezier(0.4, 0, 0.2, 1)` 为 Material Design 标准减速曲线，展开时自然平滑。

### 3.4 流式期间的思考块渲染

在流式模式下，思考块不会立即以独立 `ThoughtBlock` 组件渲染。前端 `useContentProcessor.transformSpecialBlocksForStream` 将其替换为内联 HTML 占位（第 91–111 行）：

```ts
// 流式正则替换（useContentProcessor.ts）
processed = processed.replace(
  /\[--- VCP元思考链(?::\s*([^\]]*?))?\s*---\]([\s\S]*?)\[--- 元思考链结束 ---\]/gs,
  (_, theme, content) => `\n<div class="my-2 p-3 bg-black/5 ...">...${content}...</div>\n`
);
```

**原因**：流式期间消息尚未持久化，没有完整的 `ContentBlock[]` 结构。待流结束后，Rust `stream_block_parser.rs` 的 `finalize` 将全文重新解析为正规 `ContentBlock[]`，此时思考块才以真正的 `ThoughtBlock.vue` 组件形态渲染。

---

## 4. 工具块（ToolBlock.vue）

### 4.1 职责定位

`ToolBlock.vue` 是工具调用生态的UI呈现层，区分两种形态：
- **`tool-use`**：模型发起工具调用的请求，展示工具名称与参数。
- **`tool-result`**：工具执行后的结构化结果，展示状态、键值详情与尾部备注。

### 4.2 工具调用结果展示

工具结果的解析由 Rust `content_parser.rs` / `stream_block_parser.rs` 中的 `parse_tool_result` 完成（第 713–772 行）。其语法为：

```
[[VCP调用结果信息汇总:
- 工具名称: xxx
- 执行状态: success
- 可访问URL: https://...
- 其他键: 其他值
（支持多行值）
尾部备注文本
VCP调用结果结束]]
```

解析后映射为 `details: Array<{ key, value }>` + `footer: string`。`ToolBlock.vue` 的渲染逻辑（第 86–104 行）：

```vue
<div v-for="item in block.details" :key="item.key" class="...">
  <span class="detail-key">{{ item.key }}:</span>
  <!-- 图片值直接渲染为 img -->
  <img v-if="isImageValue(item.key, item.value)" :src="item.value" />
  <div v-else class="whitespace-pre-wrap">{{ item.value }}</div>
</div>
<div v-if="block.footer" class="...">{{ block.footer }}</div>
```

**图片自动检测**：`isImageValue` 函数检测键名（`可访问URL` / `返回内容` / `url` / `image`）与值格式（HTTP URL 或 base64 data URI），自动渲染为 `<img>` 标签。

### 4.3 展开/折叠与复制

与 `ThoughtBlock` 相同，`ToolBlock` 默认折叠，点击 Header 展开。额外特性：
- **`tool-use` 未完成态**：`!block.is_complete` 时显示旋转 Loader，Header 使用蓝青渐变流动背景动画（`vcp-bubble-background-flow-kf`）。
- **IntersectionObserver 性能优化**：当工具块滚出视口时，通过 `vcp-animation-paused` 类暂停无限 CSS 动画，节省 GPU。

### 4.4 与 ToolInteractionOverlay 的关系

`ToolBlock.vue` 与 `src/features/distributed/ToolInteractionOverlay.vue` **当前无直接耦合**。

- `ToolBlock` 负责**展示**工具的文本化请求与结果，属于**被动信息渲染**。
- `ToolInteractionOverlay` 是 Phase 3 骨架，负责**交互式工具**（如相机、生物识别）的模态覆盖层，通过监听 `tool-ui-request` Tauri 事件触发。属于**主动用户交互**。

两者的分界线：若工具需要用户实时操作（拍照、确认），走 `ToolInteractionOverlay`；若工具只是返回结构化数据，走 `ToolBlock`。

---

## 5. HTML 预览块（HtmlPreviewBlock.vue）

### 5.1 职责定位

`HtmlPreviewBlock.vue` 用于渲染模型生成的 HTML 代码。它不仅是一个代码展示器，还提供了**实时预览**能力。触发条件：
- Markdown 代码围栏 ````html`
- 独立的 HTML 文档（`<!DOCTYPE html>` 或 `<html>` 开头）
- 块级 HTML 容器（`<div>`/`<section>` 等）

Rust `content_parser.rs` 将上述内容提取为 `ContentBlock::HtmlPreview`，并预先用 `syntect` 对 HTML 代码进行语法高亮，存入 `highlighted_content` 字段。

### 5.2 iframe / WebView 沙箱策略

HTML 预览采用**双层渲染策略**：

**第一层：DOMPurify 净化**
```ts
const cleanHtml = DOMPurify.sanitize(content, {
  USE_PROFILES: { html: true, svg: true, mathMl: true },
  ADD_TAGS: ['style', 'iframe', 'canvas', 'script', 'link', 'meta'],
  ADD_ATTR: ['*'],
  FORBID_TAGS: ['applet', 'embed', 'object'],
  ALLOW_UNKNOWN_PROTOCOLS: true,
  WHOLE_DOCUMENT: true,
  RETURN_DOM: false
});
```

- 允许 `script`/`style`/`iframe` 以支持交互式 HTML Demo。
- 显式禁止 `applet`/`embed`/`object` 等历史高危标签。

**第二层：iframe sandbox**
```vue
<iframe
  sandbox="allow-scripts allow-modals allow-forms allow-popups"
  loading="lazy"
  :srcdoc="getSandboxHtml(content)"
/>
```

- `allow-scripts`：允许内部 JS 运行（用于交互式 Demo）。
- **未包含** `allow-same-origin` 和 `allow-top-navigation`：防止 iframe 内脚本操作父级 DOM 或跳转顶层页面。
- 注入的 VCP 样式覆盖滚动条、背景透明、响应式图片尺寸，并拦截 `javascript:` 伪协议链接。

### 5.3 全屏预览模式（z-editor 层级）

```vue
<Teleport to="body">
  <Transition enter-active-class="transition duration-300 ease-out"
              enter-from-class="translate-y-10 opacity-0">
    <div v-show="isFullScreen" class="fixed inset-0 z-editor flex flex-col">
      <!-- 全屏 Header + Code/Preview 切换 -->
      <!-- 全屏 iframe 沙箱 -->
    </div>
  </Transition>
</Teleport>
```

- **Teleport to body**：避免被父级 `overflow: hidden` 裁剪，确保全屏覆盖整个视口。
- **z-editor 层级**：根据 `docs/UI_LAYER_ARCHITECTURE.md` 的语义化层级体系，`z-editor`（`--layer-editor: 80`）高于 `z-viewer`（70），低于 `z-toast`（90），确保全屏编辑器不会被 Toast 遮挡，但也不会覆盖 BootScreen/PermissionGate。
- **双模式 Tab**：全屏模式下支持「代码」与「预览」切换，状态与普通视图同步（`watch(isPreviewing)`）。

### 5.4 安全策略（DOMPurify、CSP）

| 层级 | 机制 | 实现 |
|------|------|------|
| 输入净化 | DOMPurify | 过滤危险标签与属性，保留结构化 HTML |
| 执行隔离 | iframe sandbox | 限制脚本能力，禁止同源访问与顶层导航 |
| 链接拦截 | JS 注入 | 拦截 `href="javascript:..."` 与空链接点击 |
| 样式隔离 | 作用域 CSS | `injectScopedCss` 将 `<style>` 块限制在 `[data-message-id]` 内 |

---

## 6. 代码块渲染

### 6.1 代码高亮实现

VCP Mobile 的代码高亮**完全由 Rust 后端在预渲染阶段完成**，前端不引入 `prismjs` 或 `highlight.js` 等前端高亮库。

**后端实现**（`src-tauri/src/vcp_modules/chat/pre_renderer/code_highlighter.rs`）：

```rust
use syntect::highlighting::ThemeSet;
use syntect::html::highlighted_html_for_string;
use syntect::parsing::SyntaxSet;

lazy_static! {
    static ref SYNTAX_SET: SyntaxSet = SyntaxSet::load_defaults_newlines();
    static ref THEME_SET: ThemeSet = ThemeSet::load_defaults();
}

pub fn highlight_code_block(code: &str, lang: &str) -> Option<String> {
    let syntax = SYNTAX_SET
        .find_syntax_by_token(&lang.to_lowercase())
        .unwrap_or_else(|| SYNTAX_SET.find_syntax_by_token("JavaScript").unwrap_or(...));
    let theme = &THEME_SET.themes["base16-ocean.dark"];
    highlighted_html_for_string(code, &SYNTAX_SET, syntax, theme).ok()
}
```

- **库**：`syntect`（Rust 版 TextMate 语法高亮引擎，与 VS Code 使用相同的 `.sublime-syntax` 定义）。
- **主题**：固定使用 `base16-ocean.dark`，与 `HtmlPreviewBlock.vue` 的暗色 GitHub 风格配色一致。
- **回退**：语言未知时回退到 JavaScript（通用类 C 语法高亮），避免无高亮的糟糕体验。

**前端消费**（`astRenderer.ts` 第 62–75 行）：

```ts
case 'code_block': {
  let html = node.highlighted_html;
  if (html) {
    // 兼容旧 AST 的嵌套 <pre> 结构，提取单层
    const nestedPreMatch = html.match(/<pre[^>]*>\s*<code>([\s\S]*?)<\/code>\s*<\/pre>/i);
    // ...
    return html;
  }
  return `<pre class="vcp-code-block vcp-scrollable"><code>${escapeHtml(node.code || '')}</code></pre>`;
}
```

### 6.2 语言检测与标识

语言标识来自 Markdown 代码围栏的 info string：

````markdown
```rust
fn main() {}
```
````

Rust `pre_renderer/markdown_parser.rs` 提取 `lang` 字段，传入 `highlight_code_block`。若 info string 为空，则 `lang = ""`，此时 `syntect` 无法匹配任何语法，触发 JavaScript 回退。

### 6.3 复制按钮

代码块的复制功能位于 `HtmlPreviewBlock.vue`（针对 HTML 代码）和未来的通用代码块组件中：

```ts
const copyCode = async () => {
  await navigator.clipboard.writeText(props.content);
};
```

当前普通 Markdown 代码块（`code_block` AST 节点）的复制功能集成在 `MessageRenderer.vue` 的上下文菜单（长按消息 → 「复制内容」），而非单代码块粒度。

---

## 7. Markdown / 文本块渲染

### 7.1 Markdown 解析策略

VCP Mobile 采用**前后端分离的 Markdown 解析架构**：

| 阶段 | 执行者 | 输出 |
|------|--------|------|
| Markdown 解析 | Rust `pre_renderer::parse_markdown_to_ast` | `MarkdownNode[]` + `InlineNode[]` |
| AST → HTML | 前端 `astRenderer.ts` | HTML 字符串 |
| 重内容渲染 | 前端 `MessageRenderer.renderHeavyContent` | KaTeX / Mermaid / 表情 DOM 操作 |

**为何不纯前端解析？**
1. **性能**：Rust 的 `pulldown-cmark` 解析速度远高于前端 JS 库。
2. **一致性**：前后端使用同一套 AST 定义，避免渲染差异。
3. **预计算**：代码高亮、哈希指纹在 Rust 端一次完成，前端只做轻量拼接。

### 7.2 KaTeX 公式渲染

**AST 表示**：Rust 解析器识别 `$...$`（行内）和 `$$...$$`（块级）为 `InlineNode::InlineMath`，序列化为：

```ts
// astRenderer.ts 第 147–151 行
case 'inline_math': {
  const isDisplay = node.display_mode || false;
  const cls = isDisplay ? 'vcp-math-block no-swipe' : 'vcp-math-inline no-swipe';
  return `<span class="${cls}" data-latex="${escapeHtml(node.content || '')}">${escapeHtml(node.content || '')}</span>`;
}
```

**动态渲染时机**：`MessageRenderer.vue` 的 `renderHeavyContent` 函数（第 166–245 行）：

```ts
const renderHeavyContent = async () => {
  if (isMessageInActiveStream.value) return; // 流式期间完全跳过
  await nextTick();
  const mathElements = messageContentRef.value.querySelectorAll('.vcp-math-inline[data-latex], .vcp-math-block[data-latex]');
  const katex = (await import('katex')).default;
  mathElements.forEach((el) => {
    if (el.querySelector('.katex')) return; // 已渲染则跳过
    katex.render(latex, el, { throwOnError: false, displayMode: isDisplay });
  });
};
```

- **按需加载**：`import('katex')` 动态导入，减少首包体积。
- **流式保护**：消息处于活跃流中时**完全不执行** KaTeX 渲染，避免流式期间每帧触发昂贵的排版计算。
- **样式**：`main.ts` 全局引入 `katex/dist/katex.min.css`；`message-blocks.css` 提供横向滚动条美化。

### 7.3 链接与表情处理

**链接**：`astRenderer.ts` 第 127–132 行：

```ts
case 'link': {
  const href = node.needs_asset_conversion && node.href
    ? convertFileSrc(node.href)   // Tauri asset:// 协议转换
    : escapeHtml(node.href || '');
  return `<a href="${href}" target="_blank" rel="noopener noreferrer">...</a>`;
}
```

- `needs_asset_conversion`：Rust 端标记本地文件路径（如图片附件），前端通过 `convertFileSrc` 转换为 WebView 安全的 `asset://` URL。

**表情（Emoticon）**：`MessageRenderer.vue` 的 `renderHeavyContent` 最后调用 `processEmoticonsInContainer`，将特定文本标记替换为 `<img>` 标签（由 `useEmoticonFixer` composable 管理）。

---

## 8. 数据流时序

### 8.1 后端 Block 解析后的渲染时序（静态消息）

```
用户打开话题 / 加载历史
        │
        ▼
┌─────────────────────┐
│ Rust: load_chat_history  │
│ → 从 DB 读取原始 content   │
│ → MessageRenderCompiler::compile() │
│   → content_parser::parse_content() │
│   → pre_renderer::parse_markdown_to_ast() │
│   → code_highlighter::highlight_code_block() │
│ → 返回 ChatMessage { blocks: ContentBlock[] } │
└──────────┬──────────┘
           │ IPC (Tauri invoke response)
           ▼
┌─────────────────────┐
│ Vue: chatHistoryStore    │
│ → 替换 message.blocks    │
└──────────┬──────────┘
           ▼
┌─────────────────────┐
│ MessageRenderer.vue      │
│ → props.message.blocks 变化 │
│ → watch() 触发 renderHeavyContent() │
│   → astRenderer.renderMarkdownNodes() │
│   → KaTeX 渲染           │
│   → Mermaid 渲染         │
│   → Emoticon 替换        │
└─────────────────────┘
```

### 8.2 流式期间 Block 增量渲染时序

```
SSE 流式 Chunk 到达
        │
        ▼
┌─────────────────────┐
│ Rust: aurora_pipeline.rs │
│ → AuroraBuffer.append_chunk(chunk) │
│ → AuroraBuffer.process_queue()     │
│   → StreamBlockParser.process(full_text) │
│   → 产出 (stable_blocks, tail)     │
│ → 推送 StreamEvent { aurora: AuroraUpdate } │
└──────────┬──────────┘
           │ IPC (Tauri Event Channel)
           ▼
┌─────────────────────┐
│ Vue: chatStreamStore     │
│ → 更新 message.blocks (stable) │
│ → 更新 message.tailBlock / tailContent │
└──────────┬──────────┘
           ▼
┌─────────────────────┐
│ MessageRenderer.vue      │
│ → stable blocks: v-memo 保证零开销 │
│ → tailBlock: 推测渲染（Speculative）│
│ → isStreaming=true: 跳过 KaTeX/Mermaid │
│ → 流结束 (isMessageInActiveStream=false) │
│   → 统一执行 renderHeavyContent()    │
└─────────────────────┘
```

---

## 9. 与 Rust 后端的 IPC 交互

### 9.1 Tauri Commands 调用表

| Command | 调用方 | 参数 | 返回 | 说明 |
|---------|--------|------|------|------|
| `process_message_content` | `useContentProcessor.ts` | `{ content: string }` | `ContentBlock[]` | 静态消息全量解析 |
| `fetch_raw_message_content` | `MessageRenderer.vue` | `{ messageId: string }` | `string` | 获取原始 Markdown 文本（用于复制/编辑） |
| `re_render_message` | `MessageRenderer.vue` | `{ messageId, topicId }` | `void` | 强制重新解析并渲染消息 |
| `load_chat_history` | `chatHistoryStore` | `{ ... }` | `ChatMessage[]` | 加载历史（已预解析 blocks） |
| `load_chat_history_streamed` | `chatHistoryStore` | `{ ... }` | `HistoryChunk[]` | 流式加载历史 |

### 9.2 事件监听表

| 事件名 | 方向 | 载荷 | 消费者 | 说明 |
|--------|------|------|--------|------|
| `vcp-stream-chunk` | Rust → 前端 | `StreamEvent` | `chatStreamStore` | SSE 流式增量推送 |
| `tool-ui-request` | Rust → 前端 | `{ tool, id, args }` | `ToolInteractionOverlay.vue` | 交互式工具 UI 请求 |
| `distributed-notification` | Rust → 前端 | `{ title, body }` | `ToolInteractionOverlay.vue` | 分布式通知 |
| `distributed-clipboard-write` | Rust → 前端 | `{ content }` | `ToolInteractionOverlay.vue` | 剪贴板写入请求 |

---

## 10. 设计决策与注意事项

### 10.1 为什么用 Block 而非纯文本渲染？

在 v0.9.x 之前的版本中，前端使用正则表达式在 `v-html` 中直接替换特殊标记。该方案存在以下问题：
1. **XSS 风险**：正则替换后的 HTML 难以彻底净化。
2. **渲染不一致**：前后端 Markdown 解析器不同，导致同一条消息在不同端显示差异。
3. **流式闪烁**：纯文本增量追加时，未闭合的 Markdown 标记会导致整段内容反复重排。

**Block 架构的优势**：
- 每个 Block 是独立的渲染单元，已闭合块通过 `v-memo` 彻底冻结，不受后续追加影响。
- Rust 端统一解析，前端只做 AST → HTML 的确定性转换。
- 特种块（tool/thought/html）获得独立组件级的交互与样式能力。

### 10.2 HtmlPreviewBlock 的 iframe 策略

**为何不直接用 `v-html` 渲染 HTML？**
- 模型生成的 HTML 可能包含 `<script>`，直接注入 DOM 会获得与主应用同源的 JS 执行权限，存在安全风险。
- iframe 的 `sandbox` 属性提供浏览器原生的执行隔离，即使允许 `allow-scripts`，脚本也无法访问 `parent.document`。

**为何允许 `allow-scripts`？**
- VCP Mobile 的使用场景中，模型常生成交互式图表（ECharts）、小游戏或动态 Demo。完全禁止脚本会严重削弱产品价值。`allow-scripts` + DOMPurify + 无 `allow-same-origin` 的组合提供了功能与安全的平衡。

### 10.3 代码高亮的性能优化

**为何选择 syntect 后端高亮而非前端库？**
- `syntect` 使用 TextMate 语法定义，覆盖 100+ 语言，质量与 VS Code 一致。
- 在 Rust 端完成高亮后，前端接收的是纯 HTML 字符串，无需加载前端高亮库、无需运行时解析语法定义。
- 对于长代码块（如数百行），后端高亮避免了前端主线程阻塞。

### 10.4 流式期间跳过重渲染的铁律

`MessageRenderer.vue` 中有多处 `if (isMessageInActiveStream.value) return;` 保护：

1. **KaTeX 渲染**：公式排版涉及大量 DOM 测量与字体计算，流式期间每帧触发会导致严重掉帧。
2. **Mermaid 渲染**：图表渲染是异步 SVG 生成，流式期间触发会造成 DOM 结构竞争。
3. **表情替换**：涉及 DOM 遍历与节点替换，与 Vue 的虚拟 DOM 更新可能冲突。

**正确性保证**：消息离开活跃流后，`watch(isMessageInActiveStream)` 确保统一执行一次 `renderHeavyContent`，补全所有重内容渲染。

### 10.5 CSS 作用域隔离

`style` 类型的 Block 允许模型注入自定义 CSS。为防止样式污染全局，前端通过 `injectScopedCss`（`useContentProcessor.ts` 第 44–78 行）将 CSS 选择器强制前缀化为 `[data-message-id="xxx"] `：

```css
/* 原始 CSS */
h1 { color: red; }

/* 注入后 */
[data-message-id="msg_123"] h1 { color: red; }
```

- `html`/`body`/:root 选择器被强制降级为 `[data-message-id]` 本身，杜绝全局污染。
- 消息卸载时调用 `removeScopedCss` 清理对应的 `<style>` 标签。

---

## 11. 术语速查表

| 术语 | 定义 |
|------|------|
| **ContentBlock** | 后端预解析的消息内容块，前端直接渲染的结构化单元。 |
| **StreamBlock** | 流式增量解析的轻量块，与 ContentBlock 同构但 hash 为字符串。 |
| **MarkdownNode** | 块级 AST 节点（paragraph / heading / code_block / table / list 等）。 |
| **InlineNode** | 行内 AST 节点（text / strong / link / image / inline_math 等）。 |
| **Aurora Pipeline** | Rust 流式语义沉淀管道，负责将 SSE chunk 解析为 stable + tail 块。 |
| **Speculative Rendering** | 流式期间对未闭合尾部（tail）的推测性 Markdown 预渲染。 |
| **syntect** | Rust 代码高亮库，基于 TextMate 语法定义。 |
| **DOMPurify** | 前端 HTML 净化库，用于 HTML 预览块的输入消毒。 |
| **v-memo** | Vue 3 编译器指令，缓存 VNode 子树，在依赖未变时跳过重渲染。 |
| **content-visibility** | CSS 属性，用于 MessageRenderer 的列表项，延迟离屏消息的渲染与布局。 |
| **LAYER_EDITOR** | UI 层级常量（z-index: 80），HtmlPreviewBlock 全屏预览使用的语义化层级。 |

---
*最后更新：2026-05-27 | VCP Mobile v0.9.14*
